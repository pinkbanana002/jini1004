# 쿠팡 로켓배송 자동화 프로그램 - 자료 분석 보고서

> **목적**: 기존 Jupyter Notebook(19개 셀)을 클로드코드 기반 **웹 애플리케이션**으로 재구축하기 위한 사전 분석 문서
>
> **원본 파일**: `로켓배송_자동화_상세대량_수강생전용_260330.ipynb` (195KB, 19 cells)
>
> **대상 사용자**: 쿠팡 로켓배송 셀러 (1688 중국 사입 → 쿠팡 로켓배송 등록 자동화)

---

## 1. 전체 아키텍처 요약

### 1.1 좌측 메뉴 구조 (최종 설계)

```
┌─────────────────────────┐
│  쿠팡 로켓배송 자동화    │
├─────────────────────────┤
│                         │
│  [ 1단계 ]              │
│   구글시트 작성          │
│   + 이미지 수정          │
│   (Cell 0 ~ Cell 6)     │
│                         │
│  [ 2단계 ]              │
│   견적서 작성            │
│   + 상품 등록            │
│   (Cell 6-1 ~ Cell 10)  │
│                         │
│  [ 추가 등록 ]          │
│   반려 상품 재작성       │
│   (Cell 11)             │
│                         │
└─────────────────────────┘
```

### 1.2 단계 간 핵심 분기점 (중요)

| 구분 | 범위 | 핵심 동작 | 종료 후 |
|------|------|-----------|---------|
| **1단계** | Cell 0 ~ Cell 6 | 1688 크롤링 → 시트 업로드 → AI 분석 | **⏸ 사용자 개입 구간 (팝업 체크리스트)** |
| **사용자 수작업** | - | ① 셀라라이프 1688 썸네일/상세이미지 수정<br>② 구글시트 데이터 최종 검수 | 체크 완료 후 2단계 진행 |
| **2단계** | Cell 6-1 ~ Cell 10 | 백업 복구 → 이미지 리사이징 → 상세페이지 제작 → 견적서 → 쿠팡 등록 → 이관 | 완료 |
| **추가 등록** | Cell 11 | 반려 상품 재작성 | 독립 메뉴 |

---

## 2. Cell별 상세 분석

### 📦 1단계 (Cell 0 ~ Cell 6)

| 순번 | 셀 ID | 역할 | 주요 라이브러리 | 외부 연동 |
|------|-------|------|----------------|-----------|
| 1 | **Cell 0** | 통합 환경설정 (API키, 시트URL, 쿠팡 ID/PW, 브랜드정보, 가격 수식) | `os` | - |
| 2 | **Cell 1** | 필수 라이브러리 자동 체크/설치 | `subprocess`, `pip` | - |
| 3 | **Cell 2** | 구글 시트 연결 및 데이터 불러오기 | `gspread`, `pandas` | 구글 시트 |
| 4 | **Cell 3** | Selenium 크롬 브라우저 실행 (프로필 유지, 다운로드 경로 고정) | `selenium`, `webdriver_manager` | Chrome |
| 5 | **Cell 4-1** | **1688 상세페이지 직접 URL 접근 크롤링** (신버전 리뉴얼 대응) | `selenium`, `deep_translator` | 1688.com |
| 6 | **Cell 4-2** | 데이터 가공 + 원가 수식 계산 + 이미지 다운로드 (멀티스레드) | `requests`, `ThreadPoolExecutor`, `PIL` | - |
| 7 | **Cell 5** | 최종 데이터를 구글시트에 업로드 (추가이미지 컬럼 포함) | `gspread_dataframe` | 구글 시트 |
| 8 | **Cell 6** | **AI 분석(Gemini) + 중국어 옵션 자동 번역** + 스마트 백업 | `google.generativeai`, `deep_translator` | Gemini API |

#### ⏸ 1단계 종료 후 팝업 체크리스트 (사용자 요구사항)

```
┌──────────────────────────────────────────────┐
│  ✅ 1단계 완료! 다음을 확인해주세요.          │
├──────────────────────────────────────────────┤
│  □ 1. 셀라라이프 1688에서 다운로드 받은      │
│        썸네일 이미지를 수정하셨나요?          │
│                                              │
│  □ 2. 셀라라이프 1688에서 다운로드 받은      │
│        상세페이지 이미지를 수정하셨나요?      │
│                                              │
│  □ 3. 구글시트에서 데이터를 최종 확인         │
│        하셨나요? (가격, 옵션명, SEO 등)       │
│                                              │
│           [모두 확인 → 2단계 시작]            │
└──────────────────────────────────────────────┘
```

**모든 체크박스가 체크되어야만 [2단계 시작] 버튼 활성화**

---

### 📦 2단계 (Cell 6-1 ~ Cell 10)

| 순번 | 셀 ID | 역할 | 주요 라이브러리 | 외부 연동 |
|------|-------|------|----------------|-----------|
| 1 | **Cell 6-1** | 백업 데이터 복구 (중복 이미지링크 순차 매칭) | `gspread` | 구글 시트 |
| 2 | **Cell 6-2** | 파일명 변경 + 400px 자동 확대 + 추가이미지 JPG 생성 | `PIL`, `shutil` | - |
| 3 | **Cell 7** | 폴더명 변경 + 파일 정리 (열린 폴더 자동 닫기, Windows PowerShell 호출) | `subprocess` | Windows |
| 4 | **Cell 7-1** | 경로 최신화 + **품질 표시 라벨 자동 생성** (브랜드/업체명/CS번호) | `PIL.ImageDraw`, `ImageFont` | - |
| 5 | **Cell 7-2** | **AI 기반 초고속 상세페이지 제작** (템플릿 + 폰트 합성) | `PIL`, `Gemini` | Gemini API |
| 6 | **Cell 8** | **서플라이허브(쿠팡) 다운로드** (PDF 제외, ZIP 자동 삭제) | `selenium`, `zipfile` | 쿠팡 |
| 7 | **Cell 9** | **견적서 엑셀 작성** (SKU 50개, 우측칸 0, 추가이미지 쉼표 구분) | `openpyxl` | - |
| 8 | **Cell 9-1** | **쿠팡 상품 등록 자동화** (상세폴더 전체 업로드, 휴식 주기 10개마다 3분) | `selenium`, `requests` | 쿠팡 서플라이허브 |
| 9 | **Cell 10** | 최종 이관 + 시트 정리 (백업 헤더 자동 추가, 엑셀 프로세스 강제 종료) | `shutil`, `gc`, `os.system` | Windows |

#### ❌ 반려 상품 등록(Cell 11)은 2단계에서 제외 → 별도 [추가 등록] 메뉴

---

### 📦 추가 등록 메뉴 (Cell 11)

| 순번 | 셀 ID | 역할 |
|------|-------|------|
| 1 | **Cell 11** | 반려 상품 재작성 — 입력한 폴더의 상품을 `완료` 시트에서 찾아 Cell 9와 동일한 최신 기준으로 견적서 덮어쓰기 |

---

## 3. 핵심 환경설정 값 (Cell 0 기반)

### 3.1 웹앱 설정 화면에서 노출해야 할 필수 입력 항목

```python
# === 구글 시트 ===
GOOGLE_JSON_FILE = 'credentials.json'   # 파일 업로드
GOOGLE_SHEET_URL = "..."                # 텍스트 입력
TARGET_SHEET_NAME = "상품등록목록"        # 텍스트 입력

# === API 키 ===
GEMINI_API_KEY = "..."                  # 비밀번호 형태 입력

# === 쿠팡 로그인 ===
SUPPLY_ID = "..."                       # 텍스트 입력
SUPPLY_PW = "..."                       # 비밀번호 형태 입력

# === 브랜드 정보 ===
MY_BRAND_NAME = "몰투데이"
MY_COMPANY_NAME = "분홍바나나"
MY_PHONE_NUMBER = "1577-7011"

# === 가격 수식 (슬라이더/숫자 입력) ===
EXCHANGE_FACTOR = 350        # 1688단가 배수
ADD_LOGISTICS_COST = 4000    # 추가 비용
SALE_PRICE_RATE = 1.67       # 판매가 배수
REC_PRICE_RATE = 1.30        # 권장가 배수
ROUND_UNIT = -2              # 반올림 단위
```

### 3.2 자동 생성되는 시스템 경로

```python
BASE_DRIVE       = os.getcwd()
PATH_TEMPLATES   = BASE_DRIVE + "/templates"       # ⚠️ 사용자가 templates 폴더 준비 필요
PATH_FONTS       = BASE_DRIVE + "/fonts"           # ⚠️ 사용자가 폰트 준비 필요
MY_DOWNLOAD_DIR  = ~/Downloads                      # 셀라라이프 1688 다운로드 기본 경로
PATH_PROFILE     = BASE_DRIVE + "/chrome_profile"  # 크롬 프로필 유지용
PATH_DOWNLOAD    = BASE_DRIVE + "/temp_downloads"
PATH_COMPLETED   = BASE_DRIVE + "/완료된_상품"
```

### 3.3 사용 폰트

```
FONT_BOLD    = "강원교육모두 Bold.ttf"
FONT_REGULAR = "강원교육모두 Light.ttf"
```

---

## 4. 필수 Python 라이브러리

```
gspread
oauth2client
pandas
requests
Pillow (PIL)
selenium
webdriver-manager
gspread-dataframe
undetected-chromedriver
openpyxl
google-generativeai
deep-translator
```

---

## 5. 외부 연동 현황

| 서비스 | 용도 | 인증 방식 |
|--------|------|-----------|
| **Google Sheets** | 상품 데이터 저장/읽기/백업 | `credentials.json` (서비스 계정) |
| **Google Gemini API** | AI SEO 분석 + 중국어 옵션 번역 + 상세페이지 제작 | API Key |
| **1688.com** | 상품 정보 크롤링 (이미지, 옵션, 가격) | 비로그인 (URL 직접 접근) |
| **쿠팡 서플라이허브** | 상품 등록 + 상세이미지 다운로드 | ID/PW |
| **셀라라이프 1688** | 썸네일/상세이미지 다운로드 (사용자 수동) | 외부 도구 |

---

## 6. 웹앱 개발 시 주요 고려사항

### 6.1 기존 Notebook → 웹앱 전환 포인트

1. **Windows 전용 코드 존재** (Cell 7, Cell 10)
   - `subprocess.run("powershell ...")` — 탐색기 창 닫기
   - `os.system("taskkill /f /im excel.exe")` — 엑셀 프로세스 종료
   - → **웹앱 서버가 Windows일 때만 동작**, 또는 해당 기능 대체/제거 필요

2. **Selenium GUI 브라우저 필요** (Cell 3, 4-1, 8, 9-1)
   - 크롬 프로필 유지 방식 사용 (`chrome_profile` 폴더)
   - → 웹앱 서버에서 백그라운드 실행 시 headless 모드 전환 검토, 또는 원격 데스크톱 방식

3. **실시간 진행상황 표시 필요**
   - Cell 4-1(크롤링), Cell 9-1(상품등록)은 수분~수십분 소요
   - → **WebSocket 또는 SSE로 실시간 로그 스트리밍** 권장

4. **1단계-2단계 사이 사용자 개입 구간**
   - 체크리스트 팝업 + 사용자 확인 후 2단계 시작
   - → 백엔드 상태값(`STAGE_1_COMPLETED`, `USER_CHECKED`) 관리 필요

5. **장시간 실행 작업**
   - Cell 9-1: 하루 50개 한도, 10개마다 3분 휴식
   - → 백그라운드 작업 큐(Celery, RQ 등) 고려

### 6.2 권장 기술 스택 (Claude Code 기반)

| 레이어 | 추천 기술 |
|--------|-----------|
| **Backend** | Python + FastAPI (기존 코드 재사용 용이) |
| **Frontend** | React 또는 Next.js (좌측 메뉴 + 메인 패널 레이아웃) |
| **실시간 통신** | WebSocket (작업 진행률, 로그 스트리밍) |
| **작업 큐** | Celery + Redis 또는 FastAPI BackgroundTasks |
| **DB** | SQLite (로컬) 또는 Supabase (유저 선호) |
| **설정 저장** | `.env` 파일 + DB (쿠팡 PW 등 민감정보 암호화) |

### 6.3 화면 UX 플로우 제안

```
[ 설정 화면 ]
   ↓ (최초 1회)
[ 메인 화면 - 좌측 메뉴 ]
   ├─ 1단계 선택 → 진행상황 로그 → [완료] → 체크리스트 팝업 → [모두 체크] → 활성화
   ├─ 2단계 선택 → (1단계 체크 완료 시만 활성화) → 진행상황 로그 → [완료]
   └─ 추가 등록 선택 → 폴더 입력 → 실행
```

---

## 7. 보안 주의사항 ⚠️

업로드된 `credentials.json`에는 다음 민감정보가 포함되어 있습니다:

- Google 서비스 계정 개인키
- 프로젝트 ID: `cupang-automation`
- 클라이언트 이메일: `my-bot@cupang-automation.iam.gserviceaccount.com`

또한 Notebook 내부에 다음 민감정보가 **하드코딩**되어 있습니다:

- Gemini API Key: `AIzaSyCuTuRY6LzfmVWgZ0tKj-Sr3gU3chCF-N0`
- 쿠팡 ID: `pinkbanana1`
- 쿠팡 PW: `pink8540@@`

**→ 웹앱 개발 시 반드시 `.env` 환경변수 + 암호화 저장소로 이전 권장**
**→ 현재 노출된 API키/비밀번호는 전량 재발급/변경 권장**

---

## 8. 개발 체크리스트 (클로드코드에 전달)

- [ ] FastAPI 백엔드 프로젝트 초기화
- [ ] 프론트엔드(React/Next.js) 좌측 메뉴 레이아웃 구축
- [ ] 설정 화면 UI + `.env` 저장 로직
- [ ] **1단계 모듈** (Cell 0~6 Python 함수화)
- [ ] **1단계 종료 시 체크리스트 팝업** 컴포넌트
- [ ] 체크리스트 3항목 모두 체크 시 2단계 버튼 활성화
- [ ] **2단계 모듈** (Cell 6-1 ~ Cell 10, 반려상품 제외)
- [ ] **추가등록 모듈** (Cell 11)
- [ ] WebSocket 기반 실시간 로그 스트리밍
- [ ] 장시간 작업을 위한 BackgroundTasks
- [ ] Windows 전용 코드 처리 (OS 분기 또는 대체 로직)
- [ ] 민감정보 암호화 저장
- [ ] 에러 핸들링 및 재시도 로직
- [ ] templates/fonts 폴더 사용자 업로드 UI

---

*분석 완료일: 2026-04-19*
